# Phase 2.3 TODO — Multi-Parser Execution Engine

- [x] Update `schemas/jobs.py` for multi-parser APIs:
- [x] Add `ParseRequest` with `parsers: list[str]`.
- [x] Update `ParseTriggerResponse` to return per-parser queued statuses.
- [x] Add `AllResultsResponse` for bulk result retrieval.
- [x] Extend `ParserStatus` with `queued_at`, `started_at`, and `completed_at`.
- [x] Rewrite `ParserService` for multi-parser orchestration:
- [x] Register all parsers but instantiate only currently available ones.
- [x] Add parser selection validation with default-to-all-available behavior.
- [x] Add `start_multi_parse()` to queue all selected parsers.
- [x] Add `run_parsers()` parallel execution with per-parser error isolation.
- [x] Add execution concurrency throttling (`min(len(parsers), 3)`).
- [x] Update `StorageService` parser status lifecycle:
- [x] Persist and return parser timestamps (`queued_at`, `started_at`, `completed_at`).
- [x] Derive job-level status from parser states (`parsing`, `completed`, `error`).
- [x] Add `get_all_results()` returning all parser results with `null` for incomplete parsers.
- [x] Update `api/v1/jobs.py` endpoints:
- [x] `POST /jobs/{job_id}/parse` accepts parser list and starts background multi-parse.
- [x] `GET /jobs/{job_id}/status` returns per-parser timeline fields.
- [x] Add `GET /jobs/{job_id}/results` endpoint for all parser outputs.
