# Phase 3.2 TODO — Comparison UX Polish

- [x] Upgrade parser result tabs to segmented tab bar with status dot, timing, and device context.
- [x] Make parser tab bar horizontally scrollable and use bottom-border active accent.
- [x] Improve split/compare parser dropdowns with completed-only options and timing metadata.
- [x] Prevent duplicate parser selection across visible split/compare panels.
- [x] Add keyboard shortcuts: `1`-`n`, `[` / `]`, `v` + `t|s|c`, and `l`.
- [x] Add shortcuts help popover in comparison header (triggered via `?` button).
- [x] Add parser panel page indicator overlay (`Page x / y`) when linked scrolling is enabled.
- [x] Improve empty/error states for no-results and split mode with insufficient completed parsers.
- [x] Add smooth transitions for view-mode and parser-content switches.
- [ ] (Stretch) Add draggable panel resize handles with persisted widths.
